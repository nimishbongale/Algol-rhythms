int main()
{
/*FILE *gnuplot = popen("gnuplot -persistent", "w");
	fprintf(gnuplot, "plot '-'\n");
	for (i = 0; i < testrun; i++)
    fprintf(gnuplot, "%g %g\n", x[i], y[i]);
	fprintf(gnuplot, "e\n");
	fflush(gnuplot);*/

return 0;
}

